# WPF-NotifyIcon-Minimal
WinAPI implementation of a notify icon (system tray) + context menu for those who don't want to use a huge nuget package or windows forms. Easy to drop in existing WPF apps or even WinUI3
